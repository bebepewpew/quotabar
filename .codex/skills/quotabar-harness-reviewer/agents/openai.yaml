interface:
  display_name: "QuotaBar Harness Review"
  short_description: "Review the guides, wrappers and workflows agents read"
  default_prompt: "Use $quotabar-harness-reviewer to review this change against docs/agent-guides/harness-review.md: run the drift snippet, then the seven checks, and report what you could not evaluate."
policy:
  allow_implicit_invocation: false
